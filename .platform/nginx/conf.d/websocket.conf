# Elastic Beanstalk (Amazon Linux 2023) nginx — enable SignalR WebSocket upgrades.
# Files in .platform/nginx/conf.d/ are included in nginx's http{} context, so this
# map is available to the proxied location in 00_application.conf.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
